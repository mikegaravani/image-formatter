# Image Formatter → Printable PDF

A small frontend-only web app that takes image files and generates a **printable PDF** where all images are placed in **uniform-sized containers** (e.g. 8 × 8 cm) with visible borders.

The entire PDF is generated **in the browser**.
